# DOM Single-element Demo

This demo intentionally animates a single DOM element.

It is CPU-bound by design and exists to demonstrate:

- Why DOM animation does not scale
- Why Canvas / WebGL is preferred for engine workloads

This is not a performance benchmark.
