# `rust-cef-runtime` Canvas GPU Demo

This demo uses the HTML Canvas API to demonstrate GPU-backed rendering without DOM overhead.

It represents the recommended rendering approach for:

- High-frequency animation
- Visualization
- Engine-style workloads

FPS reflects real main-thread throughput.
